---
import "../styles/global.css";
import { getCollection } from 'astro:content';
import Header from '../components/header.astro';
import Layout from '../layouts/Layout.astro';
import Button from "../components/button.astro";

const subject = await getCollection('subject').catch(() => []);
---

<Layout title="Particular Class">
    <Header />
<div class="fixed inset-0 -z-20 h-full w-full bg-white bg-[radial-gradient(#e5e7eb_1px,transparent_1px)] [background-size:16px_16px] opacity-20"></div>
<div class="fixed inset-0 -z-20 h-full w-full bg-white [background:radial-gradient(125%_125%_at_50%_10%,#fff_40%,#63e_100%)] opacity-15"></div>
    <nav
        class="flex flex-row items-center justify-center gap-20 w-full text-center mt-8">
        <Button title="Clases" site="/clases"></Button>
        <Button title="Cursos" site="/cursos"></Button>
        <Button title="Información" site="/info"></Button>
        <Button title="Apuntes" site="/apuntes"></Button>
    </nav>
</Layout>
